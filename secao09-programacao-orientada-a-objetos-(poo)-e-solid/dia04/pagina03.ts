const pagina03 = () => {
  // -------------------------------
  // Single Responsibility Principle
  // -------------------------------
  // SRP -> Single responsibility principle (Princípio da responsabilidade única): uma entidade (classe, método, função, etc) deve ter apenas uma única responsabilidade; 
  // conteúdo para fixar no diretorio pagina03;
}