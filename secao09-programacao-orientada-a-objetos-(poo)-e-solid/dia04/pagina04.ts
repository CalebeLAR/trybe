const pagina04 = () => {
  // ---------------------------
  // Open/Closed Principle (OCP)
  // ---------------------------
  // conteúdo para fixar no diretorio pagina03;
  // OCP -> Open/Closed principle (Princípio aberto/fechado): entidades de software devem ser abertas para extensão, mas fechadas para modificação;

  // O princípio aberto/fechado diz que:

  // Você deve ser capaz de estender um comportamento de uma entidade sem modificar seus comportamentos já existentes.  
};